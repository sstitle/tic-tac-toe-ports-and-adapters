# tictactoe-qt

Qt (PySide6) front-end for the `core` package. Install from this directory with `uv sync` or `pip install -e .` (requires `core` on the path; see `[tool.uv.sources]`).

Run: `tictactoe-qt` or `uv run tictactoe-qt`.
