# tictactoe-textual

Textual front-end for the `core` package. Install from this directory with `uv sync` or `pip install -e .` (requires `core` on the path; see `[tool.uv.sources]`).

Run: `tictactoe-tui` or `uv run tictactoe-tui`.
